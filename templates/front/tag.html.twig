{% extends 'base.html.twig' %}
    {% block body %}
<div class="tagPresentation">
    <h2>{{tag.name}}</h2>
    <p>{{tag.description}}</p>
</div>

<div class="lastPicture">

    {% for artworkTag in artworkTags %}
        <div class="card">
                <a href="{{ path('app_show', {'id': artworkTag.id}) }}"><img class="picture" src="data:image/jpeg;base64,{{ artworkTag.pictureMin }}" alt="{{ artworkTag.title }}">   
                

                    <div class="title">
                        <p>{{ artworkTag.title }} </p>
                    </div>  
                </a> 
            </div>
    {% endfor %}

</div>    
{% endblock %}